# Arrange all the characters in a given string in alphabetical order


def check(a):
    # l=[]
    # for i in a:
    #     l.append(i)
    # for i in range(len(l)):
    #     for j in range(i+1,len(l)):
    #         if l[i]>l[j]:
    #             temp=l[i]
    #             l[i]=l[j]
    #             l[j]=temp
    #     print(l[i],end='')

check('alphabet')

